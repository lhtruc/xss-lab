<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>XSS Lab - Information Security</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .card-xss {
            transition: .2s;
        }
        .card-xss:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
        }
    </style>
</head>

<body class="bg-light d-flex flex-column min-vh-100">

    <!-- Navbar -->
    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container d-flex align-items-center justify-content-between">

            <div class="d-flex align-items-center gap-2">
                <span class="navbar-brand fw-bold mb-0">
                    <a href="/" class="text-decoration-none text-white">🔐 XSS Attack Lab</a>
                </span>

                <a href="/guide"
                    class="px-2 py-0 text-white text-decoration-none rounded small">
                    Guide
                </a>
            </div>

            {{!-- Nếu user đã login thì hiện --}}
            {{#if user}}
                <div class="d-flex align-items-center gap-3">

                    <span class="text-white small">
                        👤 {{user.username}}
                    </span>

                    <a href="/users/logout" class="btn btn-sm btn-outline-light">
                        Logout
                    </a>
                </div>
            {{/if}}
        </div>
    </nav>



    <!-- Body -->
    <main class="container flex-grow-1 mb-5">
        {{{body}}}
    </main>

    <!-- Footer -->
    <footer class="mt-auto bg-dark text-secondary py-3 text-center px-4">
        <small>HCMUTE — XSS Lab — Information Security</small>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
